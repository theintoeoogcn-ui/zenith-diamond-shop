# Assets needed here

None of these were included in what you uploaded, so put your real files in
this folder with these exact names (referenced throughout the HTML):

- `zenith-logo.png`      — header brand mark (small, square-ish)
- `hero-character.png`   — home page hero illustration
- `diamond-icon.png`     — small diamond icon used on every package card
- `kbz-pay.png`          — KBZ Pay logo
- `wave-money.png`       — Wave Pay logo
- `aya-pay.png`          — AYA Pay logo

Until they're added, those `<img>` tags will just show broken-image icons —
everything else on the site (layout, animations, language switch, dark/light
mode, ordering flow) works fine without them.
